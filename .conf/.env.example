# 敏感信息配置模板 - 复制此文件为 .env 并填入真实值

# MySQL 数据库密码
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_USER=your_mysql_username
MYSQL_PASSWORD=your_mysql_password

# Mongo
MONGO_INITDB_ROOT_USERNAME=your_mongo_username
MONGO_INITDB_ROOT_PASSWORD=your_mongo_password

# Feast MySQL 连接信息 (通常与上面的MySQL信息相同)
FEAST_MYSQL_USER=your_mysql_username
FEAST_MYSQL_PASSWORD=your_mysql_password

# ClickHouse 用户密码
CLICKHOUSE_USER=your_clickhouse_username
CLICKHOUSE_PASSWORD=your_clickhouse_password
FEAST_CLICKHOUSE_USER=your_clickhouse_username
FEAST_CLICKHOUSE_PASSWORD=your_clickhouse_password

# Feast WebUI 服务配置
FEAST_UI_HOST=0.0.0.0
FEAST_UI_PORT=9999

# 通知系统 - Email SMTP 配置
# Gmail 示例: smtp.gmail.com:587
# 163 示例: smtp.163.com:465
EMAIL_SMTP_HOST=smtp.example.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=your_email@example.com
EMAIL_SMTP_PASSWORD=your_app_password
EMAIL_FROM_ADDRESS=your_email@example.com
EMAIL_FROM_NAME=Ginkgo Notification

# 使用说明:
# 1. 复制此文件: cp .env.example .env
# 2. 修改 .env 文件中的用户名和密码为真实值
# 3. .env 文件已在 .gitignore 中，不会被提交到 git
# 4. Gmail 需要使用应用专用密码: https://support.google.com/accounts/answer/185833
