import 'package:flutter/material.dart';

class MarketGinkgo extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return new Center(
      child: Text('MarketGinkgo'),
    );
  }
}